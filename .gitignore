node_modules/
dist/
__pycache__/
state/story.json
state/story.json.tmp
vite.config.ts.timestamp-*.mjs

# 本地探针书目录（跑验证时临时生成，不入仓）
.probe-book/
.probe-empty/
.probe-plan/

# 本仓 agent 工作记忆（本地态；按 docs/10 的定位不随仓分发）
.workbuddy/

# Office 临时锁文件（打开 docx/xlsx/pptx 时由 Office 生成，绝不能入仓）
~$*
