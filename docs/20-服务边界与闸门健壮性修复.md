# 20 · 服务边界与闸门健壮性修复（F12–F20）

来源：外部审查交付的漏洞清单（编号 F12–F20，接 docs/14 的 NW-P\* 体系往下排）。
执行方式：一条一个 commit，按依赖序 `F14 → F13 → F12 → F17 → F16 → F15 → F18 → F20 → F19`。

## 0. 基线动作

工作区原有一批未提交改动（写前预检 / 细纲注入 / 正文保存 / 摘要 / 规则审计 + Web 面板扩展），
先单独落成检查点 `ac608fe`，使后续每条修复能严格「一条一个 commit」。
基线状态：`pnpm -r build` 通过、core 32 项测试全绿。

## 1. 处置台账

| ID | 级别 | commit | 结论 |
|---|---|---|---|
| F14 | P0 | `c761ff8` | 已修。绑回环 + token 鉴权 + bookRoot 白名单 + 写盘默认关 |
| F13 | P0 | `1bfa9d5` | 已修。gate 子进程硬超时，超时必结算、不留孤儿 |
| F12 | P0 | `18111f0` | 已修（三处落点齐备：书根校验 / 章数对账 / 检查器退出码） |
| F17 | P1 | `45c566a` | 已修。回填只认跑前 mtime 快照 |
| F16 | P1 | `634f04b` | 部分成立。原子写**本来就有**；只摘掉了重复的那一轮 stat |
| F15 | P1 | `5f34fda` | 已修。重试层数显式化 + 熔断 + 请求数上界可审计 |
| F18 | P1 | `f66272e` | 已修。死分支换成自检；watcher 改调仓内检查器（并发现 1 个新 bug） |
| F20 | P1 | `8703195` | 已修。取消贯通到后端；前端「忙/卡」分离；proxy 假象写清 |
| F19 | P2 | `d31f163` | 已修。补第一条「应该有什么」的断言（空章 / 缺标题） |

每条 commit message 内含机制说明与实测数据，此处不复述。

## 2. 三处与任务书结论不同的地方

审查报告对下面三点的判断需要修正（其余各条的机制描述均属实）：

**① F16「writeState 是不是原子写」——是。**
`packages/core/src/state.ts` 的 `writeState` 一直是「写 `story.json.tmp` → rename」，
不存在半写 JSON 窗口。前端「数据闪一下没了」的风险不成立。
另外 `/state` 的成本是「读 1 个 `story.json` + 每章 1 次 stat」，**不是**逐章读正文；
唯一会退化成整书重读的是 `story.json` 缺失时走 `rebuildState`，属冷启动路径。
因此**没有**加 TTL 缓存。这一条不是判断，是实测（脚本造 N 章探针书后直接量 `readState`）：

| 规模 | 冷读（无 `story.json`，整书重建） | 稳态读（1 读 + N stat + 清扫） | `runGates` 全量扫描（对照） |
|---|---|---|---|
| 500 章 | 122 ms | 8 ms | 414 ms |
| 2000 章 | 445–495 ms | 23 ms | 693 ms |

面板按 5 s 轮询：稳态 8–24 ms 在噪声级；冷读最坏 495 ms 仍远小于轮询间隔，人类不可感知。
而缓存的**正确**失效判据需要对全部章 mtime 做签名（N 次 stat ≈ 稳态那一轮的成本），
等于用同样的 IO 换掉同样的 IO，还额外引入一个「可能读到旧数据」的失败类。
已有零成本替代：`novel state --book <目录> --rebuild` 落一次索引，`story.json` 此后一直在。
真正修的是重复：过闸写链原先跑两轮全量 stat（清扫 + 回填），现在正好一轮。

**② F18「watcher 那条链跑不起来」——属实，但它一直是失败关闭。**
`kit.run_child` 对「子脚本缺失」返回 **-2 而非 0**（kit.py 里专门有一段注释讲这是
曾经的假通过坑），所以 watcher 会报错而非静默放行；只是提示词把人指向不存在的
`preflight.py`。根因也不是「少一个文件」：`preflight.py` 是 `watch_and_check.py` 在
kit 里的**同级兄弟**，迁入本仓时只搬了 `consistency_check.py + kit.py + watch_and_check.py`。
另发现一个任务书未提的真 bug：`kit.run_child` 返回的是 stdout+stderr **拼接**，
而检查器是「JSON 走 stdout、摘要走 stderr」，原代码取「最后一行」当 JSON，
在任何存活的检查器上都会解析失败——已改为逐行从后往前找「带 counts 的对象」。

**③ F19「内容层零断言」——不成立，但缺口是另一种形状。**
`check()` 有实打实的内容断言（唯一写法 / 人物越界 / 数字情报矛盾 / 时间线 / 门控词 /
锁定细节 / 黑名单 / 生僻字 / 标点风格 / 章末升华 / 超长章 / 比喻堆砌 / 面板条目 /
钩子 / 伏笔 / 句长节奏）。问题是**它们全是「不该有什么」**，一条「应该有什么」都没有
——零内容的章天然全绿。即：不是缺校验，是**断言方向单一**，与协议层那个
「查不到 = 没问题」同源、只是下沉了一层。已补空章下界与缺标题两条存在性断言。

## 3. 任务书未覆盖、本轮顺带发现并修掉的

1. `apps/server` 的 `/state` 与 `/chapter` 之外，工作区新增的 `/summarize`、`/generate`、
   `/feedback`、`PUT /chapter` 同样是写盘口子——一并纳入 F14 的写盘闸门与白名单。
2. `runGates` 的 exit 失败只回显 stderr **首行**，而检查器的配置校验错误是把缺项列在
   后续行里的，导致「哪个键写错了」被截掉。已在收尾时一并修掉（`589a273`）：
   改为最多回显 6 行非空内容。
3. `content/templates/AGENTS.template.md`、`docs/00`–`docs/13` 等 **legacy 文档**仍通篇
   指导作者执行 `python tools/preflight.py` 与 `tools/start_watcher.ps1`。已核实：
   这两个文件名在**全仓都不存在**（`preflight.py` 连 `content/skills/` 里也没有，
   `*watcher*.ps1` 为零命中）。分类处置：
   - `docs/00`–`docs/13`、`legacy-SKILL.md`：按 README 定位是**旧手册存档**，重写它们等于
     篡改历史记录，保持原样。
   - `content/templates/AGENTS.template.md`、`content/skills/wuhang-long-all/4-正文/SKILL.md`：
     属随 kit 分发的**活内容**，引用不存在的脚本是真缺陷；但改写它们等于替 kit 决定
     「以后走 kit 自带 preflight 还是改调 `novel gates`」，那是架构决定而非修错，
     故本轮只记录、不改写。
4. `novel state --set`（作者标注「（保留）」，仓内零引用）能把任意 `gateStatus` 原样写盘，
   **绕过 F12 的章数对账、F17 的跑前快照语义与 `readState` 的过期清扫**——
   给一个 `{worst:"clean", checkedMtimeMs:<真实 mtime>}` 它会存活清扫并显示在面板上。
   这是本仓最后一条「不经检查就能写出绿」的路。处置：保留功能（它是作者有意留的口子），
   但补上 stderr 告警与代码注释，使代价在调用处可见；是否彻底移除留给作者一句话决定。

## 4. 遗留（不阻塞）

- `.probe-book/`、`.probe-empty/`：本轮的临时探针书目录。一次批量删除被安全确认拦下，
  按规则不再重试，故原样保留在仓根。
- legacy 文档 / kit 内容与当前仓结构的整体一致性（见 §3.3）——需先定架构取向。
- `novel state --set` 的去留（见 §3.4）——当前保留 + 告警。

## 5. 完整性核查（"是否还有没实现的" 的取证）

不是凭记忆打包票，逐条取证如下：

| 核查项 | 方法 | 结果 |
|---|---|---|
| 代码里有无未完成标记 | 全仓 grep `TODO/FIXME/XXX/HACK/未实现/待实现/not implemented/stub` | 仅 3 处误命中（字符串字面量与提示文案），**无真实待办** |
| CLI 入口是否齐全 | 逐个跑 `--help` | 10 个子命令（init/state/prompt/write/gates/generate/feedback/preflight/summarize/rules）全部就位 |
| 服务端路由是否齐全 | 起服务逐个打点 | 14 条路由全部正常应答：`/state` `/chapter` `/tasks` `/preflight` `/prompt` `/rules/audit` `/gates`（含 write）/PUT `/chapter` `/summarize` `/feedback` `/write` `/generate` `/cancel`；未知端点 404、无 token 401，**无桩、无 404 悬挂** |
| 构建 / 测试 / 类型 | `pnpm -r build`、`pnpm -r test`、`pnpm -r run typecheck` | 4 包构建通过、core 32 项测试全绿、4 包 typecheck 通过 |
| 审查清单 9 条 | 逐条对照落点 | 9/9 已实现；其中 F16 的一处落点（`/state` 缓存）经实测判定无需实施（§2①），F20-1 用服务端任务表替代了「state 里的 pending 位」 |

注：上表第 3 项用的是本轮自建的 5 章探针书 + 假 LLM 端点，覆盖了需要真写作才能走到的
`/write` 与 `/generate` 分支（后者跑完整收敛循环并落盘）。
