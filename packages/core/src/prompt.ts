import type { BuildPromptInput, PromptBundle } from './types.js';

export async function buildPrompt(input: BuildPromptInput): Promise<PromptBundle> {
  throw new Error('buildPrompt 尚未实现：当前仅建立对外契约');
}
