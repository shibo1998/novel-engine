{
  "name": "novel-engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": { "node": ">=20" },
  "scripts": {
    "typecheck": "pnpm -r build && pnpm -r run typecheck",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.16.0",
    "zod": "^3.23.0",
    "@types/node": "^20.14.0"
  }
}
